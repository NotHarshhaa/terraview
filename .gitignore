# Built binaries
/terraview
/terraview.exe
/bin/
/dist/

# Terraform artefacts in the testdata project
testdata/**/.terraform/
testdata/**/terraform.tfstate.backup

# UI build outputs (state is small; keep package-lock.json under version control)
ui/node_modules/
ui/.next/
ui/out/
ui/.turbo/

# OS / editor noise
.DS_Store
.vscode/
.idea/
*.swp

# Local clone of the separate GitHub Action repo (has its own .git)
terraview-action/

# Local CI smoke-test artefacts
terraview-report.json
terraview-report.html
